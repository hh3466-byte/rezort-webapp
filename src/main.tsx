import {StrictMode} from 'react';
import {createRoot} from 'react-dom/client';
import App from './App.tsx';
import { ErrorBoundary } from './components/ErrorBoundary.tsx';
import { initAppFreshnessWatcher } from './utils/versionCheck';
import './index.css';

// Initialize auto-refresh & cache invalidation watcher
initAppFreshnessWatcher();

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ErrorBoundary>
      <App />
    </ErrorBoundary>
  </StrictMode>,
);


