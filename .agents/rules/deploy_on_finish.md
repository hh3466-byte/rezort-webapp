# Auto Deploy on Finish

Whenever code changes are made and verified, always perform a `git add`, `git commit -m "..."`, and `git push origin main` so that the live Vercel application is updated and deployed immediately.
