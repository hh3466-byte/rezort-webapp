const fs = require('fs');
const { createClient } = require('@supabase/supabase-js');

// Load environment variables from .env
const envContent = fs.readFileSync('.env', 'utf8');
const env = {};
envContent.split('\n').forEach(line => {
  const match = line.match(/^\s*([\w.-]+)\s*=\s*(.*)?\s*$/);
  if (match) {
    let val = (match[2] || '').trim();
    if (val.startsWith('"') && val.endsWith('"')) val = val.slice(1, -1);
    if (val.startsWith("'") && val.endsWith("'")) val = val.slice(1, -1);
    env[match[1]] = val;
  }
});

const supabase = createClient(env.VITE_SUPABASE_URL, env.VITE_SUPABASE_ANON_KEY);
const MANAGER_CHAT_ID = '972543200007@c.us';

async function sendConciseReport() {
  const { data: settingsRows } = await supabase.from('settings').select('*').limit(1);
  const sRow = settingsRows?.[0] || {};
  const settings = { ...sRow, ...(sRow.data || {}) };
  const greenId = settings.greenApiIdInstance;
  const greenToken = settings.greenApiToken;

  const msg = `🟢 *אירועים ירוקים (19 אירועים שסונכרנו בהצלחה ב-24 שעות):*
• שריון לכלב *מייק* (בוריס ברנר) | תאריכים: 22.09.26-08.10.26 | שולמה מקדמה ₪2,220 | נתונים ופרטי קשר תואמים.
• שריון לכלב *ג'סי הרוטוויילרית* (ריקה נברי) | תאריכים: 22.09.26-30.09.26 | שולמה מקדמה ₪1,440 | נתונים ופרטי קשר תואמים.
• שריון לכלב *נולי* (אור נברי) | תאריכים: 07.09.26-24.09.26 | שולמה מקדמה ₪2,550 | נתונים ופרטי קשר תואמים.
• שריון לכלב *לונה המתגעגעת* (שלומי ממן) | תאריכים: 01.05.26-31.12.27 | הוסדר תשלום | נתונים ופרטי קשר תואמים.
• שריון לכלב *זומה* (מהדי (Mahdi Motors)) | תאריכים: 17.09.26-23.09.26 | שולמה מקדמה ₪1,080 | נתונים ופרטי קשר תואמים.
• שריון לכלב *ג'וי* (ירוס ביקאיה) | תאריכים: 13.08.26-07.10.26 | שולמה מקדמה ₪6,500 | נתונים ופרטי קשר תואמים.
• שריון לכלב *טר* (רעות פויר) | תאריכים: 22.09.26-01.10.26 | שולמה מקדמה ₪1,350 | נתונים ופרטי קשר תואמים.
• שריון לכלב *גו'לי* (שיין) | תאריכים: 17.09.26-22.09.26 | שולמה מקדמה ₪825 | נתונים ופרטי קשר תואמים.
• שריון לכלב *יולי* (זאב אפיק) | תאריכים: 02.10.26-04.10.26 | שולמה מקדמה ₪360 | נתונים ופרטי קשר תואמים.
• שריון לכלב *לונה* (רונן מלמוד) | תאריכים: 18.09.26-01.11.26 | שולמה מקדמה ₪4,500 | נתונים ופרטי קשר תואמים.
• שריון לכלב *לואי* (שיין) | תאריכים: 17.09.26-22.09.26 | שולמה מקדמה ₪825 | נתונים ופרטי קשר תואמים.
• שריון לכלב *זיפו* (יונתן וולפין) | תאריכים: 18.09.26-22.09.26 | שולמה מקדמה ₪720 | נתונים ופרטי קשר תואמים.
• שריון לכלב *דילן* (בר בן בסט) | תאריכים: 24.09.26-27.09.26 | הוסדר תשלום | נתונים ופרטי קשר תואמים.
• שריון לכלב *מגן* (דורין לוקס) | תאריכים: 11.09.26-29.09.26 | שולמה מקדמה ₪2,700 | נתונים ופרטי קשר תואמים.
• שריון לכלב *מרתה* (איילת פרידנזון) | תאריכים: 22.09.26-24.09.26 | שולמה מקדמה ₪360 | נתונים ופרטי קשר תואמים.
• שריון לכלב *קאיה* (עומרי וינשטיין) | תאריכים: 30.10.26-20.11.26 | הוסדר תשלום | נתונים ופרטי קשר תואמים.
• שריון לכלב *קירה* (ישראל מנדל) | תאריכים: 15.09.26-28.09.26 | שולמה מקדמה ₪1,560 | נתונים ופרטי קשר תואמים.
• שריון לכלב *תיאו* (איל שקל) | תאריכים: 06.09.26-26.10.26 | שולמה מקדמה ₪6,300 | נתונים ופרטי קשר תואמים.
• שריון לכלב *סקאי* (קובי ברי) | תאריכים: 25.09.26-27.09.26 | הוסדר תשלום | נתונים ופרטי קשר תואמים.

🚨 *אורות אדומים (3 נושאים לטיפול):*

🔴 *שריונים ללא מקדמה (₪0) שתופסים מקום ביומן (3 כלבים):*
   • 🔴 *דילן* (בר בן בסט - 053-5882051) | 24.09.26 עד 27.09.26 | ₪0 מקדמה (חוב: ₪540)
   • 🔴 *קאיה* (עומרי וינשטיין - 050-7551511) | 30.10.26 עד 20.11.26 | ₪0 מקדמה (חוב: ₪2,520)
   • 🔴 *סקאי* (קובי ברי - 054-9147080) | 25.09.26 עד 27.09.26 | ₪0 מקדמה (חוב: ₪360)

💬 *שיחות לקוחות ממתינות למענה:* אין (סוננו תגובות לד"ש, תודות וצחוק) ✅
📋 *שאלוני קליטה פתוחים:* אין (כל השאלונים נקלטו או הועברו לארכיון) ✅`;

  console.log('Sending exact concise message to Manager (972543200007@c.us)...');
  const sendRes = await fetch(`https://api.green-api.com/waInstance${greenId}/sendMessage/${greenToken}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ chatId: MANAGER_CHAT_ID, message: msg })
  });

  const sendData = await sendRes.json();
  console.log('Green-API response:', sendData);
}

sendConciseReport();
